# AI 기술 트렌드 리포트 자동화 (의료 도메인 특화)

## 프로젝트 개요
매주 최신 AI/의료 기술 동향을 자동으로 조사하여, 관심 분야별로 엄선된 논문 및 GitHub 자료를 요약한 리포트를 생성한다. 목적은 "읽어볼 만한 자료"를 빠르게 파악하는 것이며, 완전한 리뷰가 아닌 큐레이션 + 핵심 요약에 집중한다.

## 실행 주기
- **매주 월요일 오전 10:00 (KST)** 리포트 생성 및 발송
- 검색 대상 기간: 직전 리포트 발송일 이후 ~ 실행 시점까지 (최초 실행 시에는 최근 1~2주)
- 스케줄링은 Claude Code의 cron/스케줄 기능(또는 사용자가 설정한 반복 작업)을 통해 매주 월요일 09:30~10:00 사이 실행 트리거

## 중복 방지
- 검색·선정에 앞서 저장소 `reports/` 폴더의 모든 과거 리포트를 확인하여, 이미 소개된 논문(제목 또는 DOI 기준) 및 이미 소개된 GitHub 저장소를 다시 선정하지 않는다
- 후보가 이미 다뤄졌던 자료라면 건너뛰고 다음 순위 후보를 검토한다

## 검색 기간 폴백 규칙
- 기본 검색 대상 기간(위 "실행 주기" 참조) 내에서 품질 기준(자료 선정 기준 참조)을 만족하는 미보고 자료를 카테고리별로 찾지 못한 경우, **품질 기준은 낮추지 않고 검색 기간만 단계적으로 확장**한다
- 확장 순서: 기본 기간 → 최근 1개월 → 3개월 → 6개월 → 1년 → 기간 제한 없음. 조건을 만족하는 미보고 자료를 찾는 즉시 확장을 멈춘다
- 이렇게 대체된 자료는 리포트에 "최근 발행 자료 없어 과거 자료로 대체 (발행일: YYYY-MM-DD)"라고 명시한다
- 확장 없이 기본 기간에서 이미 좋은 자료를 찾았다면 이 규칙은 적용하지 않는다

## 관심 분야 (카테고리)
아래 3개 카테고리 각각에 대해 독립적으로 검색·요약한다. **모든 카테고리는 반드시 의료 데이터 또는 생명공학 데이터 기반 연구로 한정**한다 (일반 도메인 AI 논문은 제외).

1. **생물정보학 (Bioinformatics)**
   - 예: 유전체/전사체 분석, 멀티오믹스, PPI 네트워크, PRS(다유전자위험점수), 임상 데이터 전처리 관련 AI/ML 방법론
2. **LLM 관련 기술**
   - 하네스 엔지니어링(agentic harness, tool-use orchestration), 프롬프트 엔지니어링, RAG, fine-tuning 등
   - 단, 의료 QA, 임상노트 요약, 진단 보조, 의생명과학 문헌 처리 등 **의료/생명과학 응용**에 한정
3. **멀티모달 AI**
   - 영상(의료영상)-텍스트, 병리 이미지, 유전체+이미지 결합 모델 등 의료/생명공학 멀티모달 연구

## 자료 선정 기준
- **1순위**: SCIE 등재 저널 중에서도 **JCR 상위 티어(Q1, 가능하면 Impact Factor 상위권 또는 분야별 Top journal)** 논문
- **GitHub 코드가 함께 공개된 논문을 최우선** — 논문 본문/보충자료에 GitHub 링크가 명시된 경우 우선순위 최상위
- GitHub 저장소 단독 자료(논문 없이 활발히 사용되는 프로젝트)도 보조적으로 포함 가능하나, 논문 기반 자료를 우선한다
- 프리프린트(bioRxiv, medRxiv, arXiv)는 원칙적으로 제외. 단, 매우 주목할 만한 자료이고 정식 저널 출판본을 찾을 수 없는 경우에 한해 "프리프린트" 표시 후 예외적으로 포함 가능

## 분량
- 카테고리별 **1~2개 연구**만 선정 (전체 리포트 3~6개 자료)
- 자료가 부족한 주는 무리해서 채우지 말고, 있는 만큼만 보고 (품질 우선)

## 검색 전략
1. 카테고리별 핵심 키워드로 최근 1~2주 내 발행된 논문 검색
   - 생물정보학: "multi-omics deep learning clinical", "polygenic risk score machine learning", "PPI network prediction disease" 등
   - LLM: "clinical LLM agent", "medical prompt engineering", "LLM clinical notes summarization" 등
   - 멀티모달: "multimodal medical imaging genomics AI", "pathology foundation model" 등
2. 저널 임팩트/티어 확인 (JCR 등재 여부 및 Q1 여부 검색으로 교차 확인)
3. 논문 본문 또는 보충자료에서 GitHub 링크 확인
4. GitHub 저장소의 활성도(스타 수, 최근 커밋) 간단 확인

## 리포트 출력 형식
각 자료마다 다음 항목 포함:
- 제목 (원문 + 필요시 한글 설명)
- 저널명 / 발행일 / (가능하면) JCR 분류·IF
- 핵심 요약 3~5줄 (연구 목적, 방법, 주요 결과)
- GitHub 링크 (있는 경우) + 코드 활용 포인트 1줄
- 원문 링크(DOI 등)

카테고리 순서: 생물정보학 → LLM → 멀티모달 AI

## 발송
- 요약 리포트는 Markdown 형식으로 정리 후 전달 (파일 저장 또는 채팅으로 발송, 사용자 설정에 따름)
- 리포트 상단에 "이번 주 발행일 범위"와 "카테고리별 자료 수" 요약 포함
- 자동화(클라우드 routine)는 이 저장소를 clone하여 CLAUDE.md와 `reports/` 이력을 읽고, 새 리포트를 `reports/YYYY-MM-DD.md`로 커밋·푸시한 뒤 PushNotification으로 완료 요약을 전송한다
